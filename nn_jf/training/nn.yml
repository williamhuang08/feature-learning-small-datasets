dtype: "float32"
device: "cuda"

# NN hyperparameters searched on the train/validation split.
layer_list: [1, 2, 3, 4, 5]
batch_norm_list: [true, false]
lr_list: [0.1, 1.0]

# Fixed NN hyperparameters.
hidden_dim: 512
num_epochs: 2000

# Data filtering.
data_dir: "../../../../data"
max_tot: 1000

# Storage.
save_dir: "../results"