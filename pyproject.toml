# run pip install -e .
[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "feature-learning-small-datasets"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
